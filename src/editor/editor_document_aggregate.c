#include <stdint.h>

/*
 * Compatibility aggregate for the editor document family.
 * The common document logic lives in croft_editor_document_core and the
 * file-backed adapter lives in croft_editor_document_fs.
 */
const uint8_t croft_editor_document_anchor = 0;
